enum Gender { male, female, other }

enum TaskState { pending, timeOut, completed, note }

enum NoteType { sencetive, reguler }

enum ActionType {
  edit,
  delete,
  removeFromVault,
  keepInVault,
  setAsPending,
  markAsComplete,
}
